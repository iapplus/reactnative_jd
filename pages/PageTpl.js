import React from 'react';
import {SafeAreaView, ScrollView, StyleSheet} from 'react-native';

export default function PageTpc({navigation}) {
  {
    return (
      <>
        <SafeAreaView>
          <ScrollView />
        </SafeAreaView>
      </>
    );
  }
}

const styles = StyleSheet.create({});
